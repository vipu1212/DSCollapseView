source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'DSCollapseView_Example', :exclusive => true do
  pod 'DSCollapseView', :path => '../'
end

target 'DSCollapseView_Tests', :exclusive => true do
  pod 'DSCollapseView', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
